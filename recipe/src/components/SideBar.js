import React from 'react';
import './SideBar.css';

const SideBar = () => {

    return (
        <nav>
            <a href="#">Shop</a>
            <a href="#">About</a>
        </nav>
    )

}

export default SideBar;
